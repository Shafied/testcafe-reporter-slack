export const bold = (text) => `*${text}*`;

export const italics = (text) => `_${text}_`;

export const strike = (text) => `~${text}~`;

export const code = (text) => `\`${text}\``;

export const quote = (text) => `>${text}`;
